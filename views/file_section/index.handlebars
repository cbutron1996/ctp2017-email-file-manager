<head>
  <link rel="stylesheet" type="text/css" href="/css/file_section.css">
</head>
  <a href="http://localhost:8000/attachments/{{this.page}}?search={{this.search}}">Page {{this.page}}</a>
  <table class="table table-responsive">
    <thead>
      <tr>
        <th>File Type</th>
        <th>File Name</th>
        <th>Download</th>
        <th>Email</th>
      </tr>
    </thead>
    <tbody>
      <!-- example to create a row -->
      <!-- <tr>
        <td>Juldy</td>
        <td>Dooley</td>
        <td>july@example.com</td>
      </tr> -->
      {{#each attachments}}
        <tr>
          <td>
            <img src="/img/512px/{{this.file_type}}.png" alt="Logo" class="small_log">
          </td>
          <td>
            <a href="attachments/{{this.message_id}}/{{this.attachment_id}}/{{this.file_name}}" target="_blank">
              {{this.file_name}}
            </a>
          </td>
          <td>
            <a href="attachments/{{this.message_id}}/{{this.attachment_id}}/download/{{this.file_name}}" target="_blank">
              Download
            </a>
          </td>
          <td><a href="emails?search={{this.message_id}}">Link</a></td>
        </tr>
      {{/each}}
    </tbody>
  </table>
